<?php
//REAADING FROM A JSON FILE AND ALSO ADDING DATA TO THE JSON FILE
    // $fp=fopen("file.json",'r+');
    // $data=fread($fp,filesize("file.json"));
    // $arr=json_decode($data);
    // print_r($arr);
    // fclose($fp);

    // $fp=fopen("file.json","r+");
    // $data=array("name"=>"Zalim Khan","rollno"=>"20-arid-502");
    // fseek($fp,-2,SEEK_END);
    // fwrite($fp,",".json_encode($data)."]");
    // fclose($fp);
    // print_r($data);
?>
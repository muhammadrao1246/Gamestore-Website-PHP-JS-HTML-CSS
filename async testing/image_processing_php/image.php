<?php
    // CREATING IMAGE ASSIGNING BG AND TXT COLOR AND DISPLAYING
    // $image = imagecreate(500,500);
    // $bg = imagecolorallocate($image,0,255,0);
    // $txt = imagecolorallocate($image,255,255,255);
    // imagestring($image,15,200,200,'My Name Is Muhammad Bin Zulfiqar',$txt);
    // imagepng($image); 
    // imagedestroy($image);

    //FETCHING FILE FROM DIRECTORY AND CROPPING IT W.R.T INTRINSIC RESOLUTION
    // $image=imagecreatefromjpeg('/xampp/htdocs/practice/images/john wick.jpg');
    // $second=imagecrop($image,['x'=>200,'y'=>200,'width'=>5000,'height'=>5000]);

    // header('Content-Type: image/png');
    
    // imagepng($second);
    
    // imagedestroy($second);
    // imagedestroy($image);

    //RESIZING IMAGE WHULE PRESERVING ASPECT RATIO
    // $image=imagecreatefromjpeg('/xampp/htdocs/practice/images/john wick.jpg');
    // $image=imagescale($image,500,-1);
    // header('Content-Type: image/png');    
    // imagepng($image);    
    // imagedestroy($image);

    //READING IMAGE AND DISPLAYING IN A HTML DOCUMENT
    // $fp = fopen('/xampp/htdocs/practice/images/john wick.jpg','r');
    // $data = fread($fp,filesize('/xampp/htdocs/practice/images/john wick.jpg'));
    // //  echo '<img src="data:image/*;base64,'.base64_encode($data).'" width=500 />';
    
    // $fp = fopen('/xampp/htdocs/practice/async testing/ajax tutorials/Ajax XML/data.xml','r');
    // $data = fread($fp,filesize('/xampp/htdocs/practice/async testing/ajax tutorials/Ajax XML/data.xml'));

    

?>